# Tests

Planned coverage:

- schema and coordinate validation;
- scoring determinism;
- hard-gate behaviour;
- missing-data penalties;
- source-adapter fixtures;
- temporal and spatial edge cases;
- regression tests for known recommendation runs.
