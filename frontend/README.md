# Frontend

Planned home of GeoWeaver's interactive map, recommendation report, filters, and field-observation interface.

The first milestone may use generated Markdown or a minimal local web interface. Avoid committing to a large frontend stack before validating the recommendation workflow.
