# Backend

Planned home of the GeoWeaver domain model, source adapters, feature extraction, constraint evaluation, scoring, and recommendation services.

The weekend MVP should begin as a small Python package rather than a networked service. Add an API only after the domain contracts are stable.
