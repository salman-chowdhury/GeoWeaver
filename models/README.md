# Models

Store versioned rule sets, trained-model metadata, evaluation results, and model documentation here.

Do not commit large model weights directly. The initial model is a transparent deterministic scorer with explicitly versioned weights.
